# README Entrypoint

## Purpose
TBD.

## Requirements

### Requirement: README defines a minimal runnable loop
The README SHALL describe a minimal runnable loop with install and run steps.

#### Scenario: New contributor onboarding
- **WHEN** a contributor reads the README
- **THEN** they can follow the steps to run the minimal loop
