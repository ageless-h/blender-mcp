# Protocol Alignment

## Purpose
TBD.

## Requirements

### Requirement: Protocol documentation matches implementation
The project SHALL document the minimal JSON-line loop if the official MCP SDK is not used.

#### Scenario: Protocol review
- **WHEN** a contributor reads protocol documentation
- **THEN** it matches the implemented JSON-line loop behavior
