# Minimum Capability Registration

## Purpose
TBD.

## Requirements

### Requirement: Minimal capability set is registered
The example startup SHALL register the minimal capability set into the catalog.

#### Scenario: Example startup
- **WHEN** the stdio example starts
- **THEN** the catalog contains the minimal capability set
